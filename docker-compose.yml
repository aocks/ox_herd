version: '2'
services:
  
  ox_server:
    build:
      context: .
      args:
        - GITHUB_USER
        - GITHUB_TOKEN
        - OX_WEB_USER
        - OX_PASSWD_HASH
    ports:
      - '6617:6617'
    image: ox_herd_image
    container_name: ox_server

